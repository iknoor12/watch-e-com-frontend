# Client (React)

This folder contains the React frontend built with Vite and Tailwind CSS. It includes placeholder components, pages, contexts, and utilities for the Watches E-commerce website.

Run:

1. npm install
2. npm run dev
# watch-e-com-frontend
